{
  "name": "schedu-terp-app",
  "scripts": {
    "build": "cd client && npm install && npm run build && cd ..",
    "deploy": "cp -a client/build/. public/",
    "postinstall": "npm run build && npm run deploy && echo 'Client built!'"
  },
  "dependencies": {
    "bootstrap": "^4.3.1",
    "reactstrap": "^6.5.0"
  }
}
